import {Primitives} from "@codelytv/primitives-type";

export class Job {
    static create({
                      id,
                      title,
                      companyName,
                      location,
                      type,
                      description,
                  }: Primitives<Job>): Job {
        return;
    }
}